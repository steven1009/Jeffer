@model IEnumerable<Jeffer_FERRETERIA.Models.proveedores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoProveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdDetalleProvProd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoProveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdDetalleProvProd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProveedores }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProveedores }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProveedores })
        </td>
    </tr>
}

</table>
